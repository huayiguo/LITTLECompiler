PROGRAM p
BEGIN

	INT a,b,c;
	STRING e := " ";
	FUNCTION VOID main()
	BEGIN
		a := 20;
		b := 30;
		c := 40;

		c := c + a*b + (a*b+c)/a + 20;
		b := b*b + a;
		a := (b*a)/a;

		WRITE (c);
		WRITE (e);
		WRITE (b);
		WRITE (e);
		WRITE (a);

	
	END
END